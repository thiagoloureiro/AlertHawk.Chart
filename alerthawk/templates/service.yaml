{{- range $component, $image := .Values.image }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "alerhawk.fullname" $ }}-{{ $component }}
spec:
  type: ClusterIP
  ports:
    - port: {{ $.Values.service.port }}
      targetPort: {{ $.Values.service.port }}
  selector:
    app: {{ include "alerhawk.name" $ }}
    component: {{ $component }}
{{- end }}
